# Tidebound Fleet 开发宪法

本文件适用于仓库根目录及全部子目录。用户在当前任务中的明确要求优先于本文件；发生冲突时先说明影响，再按用户要求执行。

## 项目目标

Tidebound Fleet（潮汐舰队）是一款面向 Android / iOS、Google Play 与 TapTap 的原创移动解谜游戏。核心是四方向单向挪船、外围航道、同型舰队聚合及每船一次的 Boss 攻击反馈。

正式 Unity 工程唯一位置：`10_开发工作区/TideboundFleet_Unity/`。购买源码与 Cocos 参考项目只用于审计和研究，不能成为正式代码写入位置。当前产品采用约80艘的高密度方向疏通、1×2可换皮标准船和固定外观1×3长船。

## 当前阶段与禁止事项

阶段顺序：Phase 0 源码审计；Phase 0.5 游戏设计冻结；Phase 0.8 项目治理；Phase 1 Unity架构；Phase 2棋盘；Phase 3船移动；Phase 4航道；Phase 5单船迁移与80船灰盒；Phase 6关卡验证与生产工具；Phase 7舰队与海怪战斗；Phase 8道具、死局与重开；Phase 9皮肤经济；Phase 10产品整合；Phase 11 IAA与分析；Phase 12发布。

在任何任务中都要遵守用户声明的阶段边界。Phase 5 的单基础船、12×18高密度棋盘、容量校验、密集输入、Level Studio V0和自动化灰盒测试已经完成。进入 Phase 6 前先确认真机触控、方向可读性和最低设备性能门槛；不得提前实现Boss战斗、皮肤经济、广告或正式UI。

永久禁止：

- 修改 `00_远端接收区/` 内的外部原始材料；需要试验时复制到明确的工作副本。
- 在 `Assets` 根目录创建 `Test`、`Temp`、`New`、`Backup`。
- 把密码、数据库连接串、API Key、签名文件、账号信息或第三方私密 SDK 配置写进 Git、代码、Markdown、日志或截图。
- 将 Unity 的 Library、Temp、Obj、Logs、UserSettings、Build/Builds 或移动端安装包提交到 Git。
- 把临时截图、导出日志、一次性脚本或分析中间文件散落在正式目录。
- 未经明确要求连接数据库、运行迁移、修改线上数据、发布构建或推送远端。

## 文件规则

- 正式代码和 Unity 配置：`10_开发工作区/TideboundFleet_Unity/`。
- Unity 测试：对应模块的 `Tests/EditMode` 或 `Tests/PlayMode`；不得放在 Assets 根目录。
- 游戏设计、美术源文件和音频：`30_设计与素材库/` 下对应分类。
- 架构、设计基线、开发计划、变更记录、API 说明和发布记录：`40_项目交接文档/`。
- 本地密钥与连接信息：`90_本地私密配置_不提交/`；仅提供无凭据的根目录 `.env.example`。
- 临时分析和一次性文件：`99_垃圾存储区/YYYYMMDD_用途...`。创建前确认确有用途；引用结束后删除。每周人工检查并清理超过 30 天且无引用的文件。
- 外部收到的源码、资源、压缩包和参考文件：`00_远端接收区/`。先审计、记录来源和许可，再决定是否进入正式素材库。
- 构建产物：`20_交付构建/`，按 Android/APK、Android/AAB、iOS、测试包归档；全部不提交 Git。

## 修改前说明

修改任何正式代码或配置前，先向用户说明：

1. 将修改的文件或目录。
2. 修改原因。
3. 对数据、模块、平台和测试的影响范围。

说明应具体到可评审范围。不得先生成大量文件再补充解释。

## 开发与验证流程

1. 读取 `40_项目交接文档/GAME_DESIGN.md`、`ARCHITECTURE.md` 和与任务相关的验证记录。
2. 检查 Git 状态，保护用户已有改动；不得用破坏性命令覆盖无关修改。
3. 只在当前阶段范围内实施，保持 `Tidebound.*` namespace 和 asmdef 模块边界，不引用旧停车玩法代码。
4. 视觉尺寸与逻辑 Grid 占格分离。逻辑不得读取 Transform、Renderer 或 Collider 尺寸决定占格。
5. 为有风险的纯逻辑改动补充有意义的 EditMode 测试；动画、场景和输入集成按需使用 PlayMode 测试。
6. 运行与改动相称的测试，记录真实结果；不得把“未运行”写成“通过”。
7. 更新 `40_项目交接文档/CHANGELOG.md` 的 `[Unreleased]`，并在交付时报告改动、验证、风险和下一步。

## Git 规则

- 默认分支为 `main`。日常工作使用短生命周期分支，如 `feat/board-grid`、`fix/ship-occupancy`、`docs/project-governance`。
- 提交标题使用 `<type>(<scope>): <简明说明>`；允许类型：`feat`、`fix`、`refactor`、`docs`、`asset`、`build`、`test`、`chore`。
- 一次提交只表达一个可回滚意图。提交前检查 `git diff --check`、敏感信息、生成物和相关测试。
- 不提交空目录占位以外的无用途文件，不提交注释掉的大段废弃实现，不提交本地绝对路径到产品配置。
- 不擅自执行 `git push`、强制推送、改写公共历史或创建发布标签；这些操作需要用户明确要求。

## 数据库与外部服务

- 应用代码只能通过环境变量或平台安全配置读取连接信息，禁止硬编码。
- 本地变量统一放在 `90_本地私密配置_不提交/.env`。仓库只提交 `.env.example`，且示例不得含真实主机、用户或密码。
- 日志和异常不得打印完整连接串；需要诊断时最多报告变量是否存在及非敏感错误类别。
- 当前基础游戏离线可运行。数据库、广告、归因或登录服务失败不得成为进入核心游戏的前置条件。
